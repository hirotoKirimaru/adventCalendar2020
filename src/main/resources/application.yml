spring:
  profiles:
    active:
      - local
    group:
      common:
        - common-cron
        - common-mybatis
        - common-thymeleaf
      local:
        - database-local
        - external
        - common
      win:
        - database-local-win
        - external
        - common
      external:
        - external-dummy-user
#  config:
#    use-legacy-processing: true
